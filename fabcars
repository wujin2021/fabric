CREATE DATABASE IF NOT EXISTS fabcars;

USE fabcars;

CREATE TABLE IF NOT EXISTS cars (
    `key` VARCHAR(100) PRIMARY KEY,
    `make` VARCHAR(100),
    `model` VARCHAR(100),
    `colour` VARCHAR(100),
    `owner` VARCHAR(500)
);